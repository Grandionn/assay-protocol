{
  "name": "assay-discovery-api",
  "version": "1.0.0",
  "description": "Semantic agent discovery — natural language queries matched to on-chain agents, ranked by embedding similarity × Assay Score × stake",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node scripts/seed.js",
    "test": "jest --forceExit --runInBand"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "cors": "^2.8.6",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 15000
  }
}
